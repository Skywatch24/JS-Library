// status for next archive video
const STATUS_OK = 'ok';
const STATUS_END = 'end';
const STATUS_HOLE = 'hole';

const STREAM_TYPE = {
  flv: 'flv',
  hls: 'hls',
};

export {STATUS_OK, STATUS_END, STATUS_HOLE, STREAM_TYPE};
