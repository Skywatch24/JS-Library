import React from 'react';
import ReactDOM from 'react-dom';

const APP = () => {
  return <div>123</div>;
};

ReactDOM.render(
  <React.StrictMode>
    <APP />
  </React.StrictMode>,
  document.getElementById('root'),
);
